apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: chatflow
  labels:
    app: frontend
spec:
  type: LoadBalancer      # External access for users
  selector:
    app: frontend
  ports:
  - port: 80              # Standard HTTP port
    targetPort: 80        # nginx listens on 80
    protocol: TCP
    name: http